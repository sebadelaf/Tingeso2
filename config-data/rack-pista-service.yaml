server:
  port: 0
spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL_RMF6}
    username: postgres
    password: 1234
eureka:
  instance:
    preferIpAddress: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: ${EUREKA_SERVER_URL}